---
globs: eslint.config.js
alwaysApply: false
---

# eslint.config.js

## Obrigatório

- `@typescript-eslint/no-explicit-any`: `'error'` (nunca `'warn'`)

## Configuração

```javascript
rules: {
  '@typescript-eslint/no-explicit-any': 'error',
  '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_', varsIgnorePattern: '^_' }]
}
```
